<button class="bnt btn-danger pull-right" id="drop_image"><i class="fa fa-trash"></i> Supprimer l'image</button>
<div style="clear: both;"></div>
<form action="{{ path('upload_file') }}" method="POST" id="formlogo">
<div class="form-group">
    <label>{{ title }}</label>
    <input type="file" name="filelogo" class="form-control" data-element="{{ key }}" >
</div>
</form>